/*document.addEventListener("mousemove", (e) => {
    const posX = e.clientX;
    const posY = e.clientY;
    const pageWidth = Math.max(html.clientHeight);
    const pageHeight = Math.max(html.clientWidth)
    const percentPosX = (posX / pageWidth) *;

});*/